import { AppBar, Toolbar, Typography } from "@mui/material";

export default function Menu() {
    return (
        <AppBar position="static">
            <Toolbar>
                <Typography>Felipe Blanco</Typography>
            </Toolbar>
        </AppBar>
    )
}